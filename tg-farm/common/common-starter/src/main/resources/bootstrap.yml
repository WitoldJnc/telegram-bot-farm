spring:
  application:
    name:
  cloud:
    consul:
      host:
      port:
      config:
        enabled:
        prefix:
        defaultContext:
      discovery:
        register:
        healthCheckPath:
        healthCheckInterval:
        tags:
  kafka:
    consumer:
      bootstrap-servers:
      group-id:
      auto-offset-reset:
      key-deserializer:
      value-deserializer:
    producer:
      topic:
      id:
      bootstrap-servers:
      key-serializer:
      value-serializer:
