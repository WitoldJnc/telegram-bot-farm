spring:
  application:
    name:
  cloud:
    consul:
      host:
      port:
      config:
        enabled:
        prefix:
        defaultContext:
      discovery:
        register:
        healthCheckPath:
        healthCheckInterval:
        tags:
