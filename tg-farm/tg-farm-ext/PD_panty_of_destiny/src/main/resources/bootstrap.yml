spring:
  application:
    name: pod_panty_of_destiny_bot
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        prefix:
        defaultContext: ph.panty_of_destiny.bot